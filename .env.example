# Local, non-secret example configuration. PostgreSQL connection values are set
# explicitly by your local runtime; DuckDB remains the safe default.
MM_DATABASE_URL=duckdb:///data/local/marketing.duckdb
MM_DATA_DIR=data
MM_RANDOM_SEED=20260910
